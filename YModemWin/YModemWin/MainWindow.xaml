<Window x:Class="YModemWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="YModem WPF"
        Height="620"
        Width="1120"
        MinHeight="580"
        MinWidth="1040">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <GroupBox Grid.Row="0" Header="Serial" Padding="12" Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="Port" />
                <ComboBox x:Name="PortComboBox" Width="180" Margin="8,0,0,0" />
                <Button Content="Refresh" Margin="8,0,0,0" Padding="12,4" Click="OnRefreshPortsClick" />
                <TextBlock VerticalAlignment="Center" Margin="16,0,0,0" Text="Baud" />
                <ComboBox x:Name="BaudRateComboBox" Width="140" Margin="8,0,0,0" IsEditable="True">
                    <ComboBoxItem Content="9600" />
                    <ComboBoxItem Content="19200" />
                    <ComboBoxItem Content="38400" />
                    <ComboBoxItem Content="57600" />
                    <ComboBoxItem Content="115200" />
                    <ComboBoxItem Content="230400" />
                    <ComboBoxItem Content="460800" />
                </ComboBox>
                <CheckBox x:Name="SendTimeoutCheckBox" Margin="16,2,0,0" IsChecked="True" Content="Send timeout" />
                <CheckBox x:Name="ReceiveTimeoutCheckBox" Margin="12,2,0,0" IsChecked="True" Content="Receive timeout" />
                <Button Content="Cancel Current Task" Margin="20,0,0,0" Padding="12,4" Click="OnCancelClick" />
            </StackPanel>
        </GroupBox>

        <GroupBox Grid.Row="1" Header="Send" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal">
                    <Button Content="Add Files" Padding="12,4" Click="OnBrowseSendFileClick" />
                    <Button Content="Clear" Margin="8,0,0,0" Padding="12,4" Click="OnClearSendFilesClick" />
                    <Button Content="Start Send" Margin="8,0,0,0" Padding="12,4" Click="OnStartSendClick" />
                </StackPanel>

                <ListBox x:Name="SendFilesListBox" Grid.Row="1" Height="90" Margin="0,8,0,0" />
                <ProgressBar x:Name="SendProgressBar" Grid.Row="2" Minimum="0" Maximum="100" Height="16" Margin="0,8,0,0" />
                <TextBlock x:Name="SendStatusTextBlock" Grid.Row="3" Margin="0,6,0,0" Text="Send status: idle" />
            </Grid>
        </GroupBox>

        <UniformGrid Grid.Row="2" Columns="4" Margin="0,0,0,10">
            <TextBlock x:Name="SendBytesTextBlock" Text="Send bytes: 0/0" />
            <TextBlock x:Name="SendPacketsTextBlock" Text="Send packets: 0/0" />
            <TextBlock x:Name="ReceiveBytesTextBlock" Text="Receive bytes: 0/0" />
            <TextBlock x:Name="ReceivePacketsTextBlock" Text="Receive packets: 0/0" />
        </UniformGrid>

        <GroupBox Grid.Row="3" Header="Receive" Padding="12" Margin="0,0,0,10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBox x:Name="SaveFolderTextBox" Grid.Column="0" Margin="0,0,8,0" />
                <Button Grid.Column="1" Content="Browse" Padding="12,4" Click="OnBrowseSaveFolderClick" />
                <Button Grid.Column="2" Content="Start Receive" Margin="8,0,0,0" Padding="12,4" Click="OnStartReceiveClick" />
            </Grid>
        </GroupBox>

        <ProgressBar x:Name="ReceiveProgressBar" Grid.Row="4" Minimum="0" Maximum="100" Height="16" />
        <TextBlock x:Name="ReceiveStatusTextBlock" Grid.Row="5" Margin="0,6,0,10" Text="Receive status: idle" />

        <UniformGrid Grid.Row="6" Columns="2" Margin="0,0,0,10">
            <TextBlock x:Name="ReceiveFileNameTextBlock" Text="File: -" />
            <TextBlock x:Name="ReceiveFileDateTextBlock" Text="Date: -" />
        </UniformGrid>

        <DockPanel Grid.Row="7" Margin="0,0,0,8">
            <TextBlock DockPanel.Dock="Left" Text="Runtime Log" FontWeight="Bold" />
            <Button DockPanel.Dock="Right" Content="Clear Log" Padding="10,2" Click="OnClearLogClick" />
        </DockPanel>

        <TextBox x:Name="RuntimeLogTextBox"
                 Grid.Row="8"
                 IsReadOnly="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 HorizontalScrollBarVisibility="Disabled"
                 AcceptsReturn="True" />
    </Grid>
</Window>
