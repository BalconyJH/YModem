<ui:FluentWindow x:Class="YModemWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="YModem WPF"
        Height="620"
        Width="1120"
        MinHeight="580"
        MinWidth="1040"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <ui:FluentWindow.Resources>
        <Style x:Key="UniformComboBoxStyle" TargetType="ComboBox">
            <Setter Property="Width" Value="180" />
            <Setter Property="Height" Value="34" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>

        <Style x:Key="UniformButtonStyle" TargetType="ui:Button">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>

        <Style x:Key="UniformToggleButtonStyle" TargetType="ui:ToggleButton">
            <Setter Property="Height" Value="34" />
            <Setter Property="MinWidth" Value="120" />
            <Setter Property="Padding" Value="12,4" />
            <Setter Property="VerticalAlignment" Value="Center" />
        </Style>
    </ui:FluentWindow.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <!-- Title Bar -->
        <ui:TitleBar Grid.Row="0" Title="YModem WPF" />

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="200" />
            </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Margin="0,0,0,10"
                Padding="12"
                CornerRadius="8"
                Background="#2B303A">
            <StackPanel>
                <TextBlock Margin="0,0,0,8" FontWeight="SemiBold" Text="Serial" />
                <StackPanel Orientation="Horizontal">
                <TextBlock VerticalAlignment="Center" Text="Port" />
                <ComboBox x:Name="PortComboBox" Margin="8,0,0,0" Style="{StaticResource UniformComboBoxStyle}" />
                <ui:Button Content="Refresh" Margin="8,0,0,0" Style="{StaticResource UniformButtonStyle}" Click="OnRefreshPortsClick" />
                <TextBlock VerticalAlignment="Center" Margin="16,0,0,0" Text="Baud" />
                <ComboBox x:Name="BaudRateComboBox" Width="180" Margin="8,0,0,0" IsEditable="True" Style="{StaticResource UniformComboBoxStyle}">
                    <ComboBoxItem Content="9600" />
                    <ComboBoxItem Content="19200" />
                    <ComboBoxItem Content="38400" />
                    <ComboBoxItem Content="57600" />
                    <ComboBoxItem Content="115200" />
                    <ComboBoxItem Content="230400" />
                    <ComboBoxItem Content="460800" />
                </ComboBox>
                <ui:CheckBox x:Name="SendTimeoutCheckBox" Margin="16,2,0,0" IsChecked="True" Content="Send timeout" />
                <ui:CheckBox x:Name="ReceiveTimeoutCheckBox" Margin="12,2,0,0" IsChecked="True" Content="Receive timeout" />
                </StackPanel>
            </StackPanel>
        </Border>

        <Grid Grid.Row="1" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="16" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" Padding="12" CornerRadius="8" Background="#2B303A">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                    </Grid.RowDefinitions>

                    <StackPanel Grid.Row="0" Orientation="Horizontal">
                        <TextBlock Margin="0,0,16,0" VerticalAlignment="Center" FontWeight="SemiBold" Text="Send" />
                        <ui:Button Content="Add Files" Style="{StaticResource UniformButtonStyle}" Click="OnBrowseSendFileClick" />
                        <ui:Button Content="Clear" Margin="8,0,0,0" Style="{StaticResource UniformButtonStyle}" Click="OnClearSendFilesClick" />
                        <ui:ToggleButton x:Name="SendActionButton" Content="Standard ToggleButton" Margin="8,0,0,0" Style="{StaticResource UniformToggleButtonStyle}" Click="OnStartSendClick" />
                    </StackPanel>

                    <ListBox x:Name="SendFilesListBox" Grid.Row="1" Margin="0,8,0,8" />

                    <ProgressBar x:Name="SendProgressBar" Grid.Row="2" Minimum="0" Maximum="100" Height="16" />
                    <TextBlock x:Name="SendStatusTextBlock" Grid.Row="3" Margin="0,6,0,6" Text="Send status: idle" />

                    <UniformGrid Grid.Row="4" Columns="2">
                        <TextBlock x:Name="SendBytesTextBlock" Text="Send bytes: 0/0" />
                        <TextBlock x:Name="SendPacketsTextBlock" Text="Send packets: 0/0" />
                    </UniformGrid>
                </Grid>
            </Border>

            <Border Grid.Column="2" Padding="12" CornerRadius="8" Background="#2B303A">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="Auto" />
                        <RowDefinition Height="*" />
                    </Grid.RowDefinitions>

                    <TextBlock Grid.Row="0" Margin="0,0,0,8" FontWeight="SemiBold" Text="Receive" />

                    <Grid Grid.Row="1">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>

                        <ui:TextBox x:Name="SaveFolderTextBox" Grid.Column="0" Margin="0,0,8,0" Height="34" VerticalContentAlignment="Center" />
                        <ui:Button Grid.Column="1" Content="Browse" Style="{StaticResource UniformButtonStyle}" Click="OnBrowseSaveFolderClick" />
                        <ui:ToggleButton x:Name="ReceiveActionButton" Grid.Column="2" Content="Standard ToggleButton" Margin="8,0,0,0" Style="{StaticResource UniformToggleButtonStyle}" Click="OnStartReceiveClick" />
                    </Grid>

                    <ProgressBar x:Name="ReceiveProgressBar" Grid.Row="2" Minimum="0" Maximum="100" Height="16" Margin="0,8,0,0" />
                    <TextBlock x:Name="ReceiveStatusTextBlock" Grid.Row="3" Margin="0,6,0,6" Text="Receive status: idle" />

                    <UniformGrid Grid.Row="4" Columns="2">
                        <TextBlock x:Name="ReceiveBytesTextBlock" Text="Receive bytes: 0/0" />
                        <TextBlock x:Name="ReceivePacketsTextBlock" Text="Receive packets: 0/0" />
                    </UniformGrid>

                    <UniformGrid Grid.Row="5" Columns="2" Margin="0,8,0,0">
                        <TextBlock x:Name="ReceiveFileNameTextBlock" Text="File: -" />
                        <TextBlock x:Name="ReceiveFileDateTextBlock" Text="Date: -" />
                    </UniformGrid>
                </Grid>
            </Border>
        </Grid>

        <Border Grid.Row="2" Padding="12" CornerRadius="8" Background="#2B303A">
            <DockPanel>
                <TextBlock DockPanel.Dock="Top" Margin="0,0,0,8" FontWeight="SemiBold" Text="Runtime Log" />
                <ui:Button DockPanel.Dock="Top"
                        HorizontalAlignment="Right"
                        Content="Clear Log"
                        Style="{StaticResource UniformButtonStyle}"
                        Margin="0,0,0,8"
                        Click="OnClearLogClick" />
                <ui:TextBox x:Name="RuntimeLogTextBox"
                         IsReadOnly="True"
                         TextWrapping="Wrap"
                         VerticalScrollBarVisibility="Auto"
                         HorizontalScrollBarVisibility="Disabled"
                         AcceptsReturn="True" />
            </DockPanel>
        </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
