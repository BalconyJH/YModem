<ui:FluentWindow x:Class="YModemWin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="YModem WPF"
        Height="620"
        Width="1120"
        MinHeight="580"
        MinWidth="1040"
        ExtendsContentIntoTitleBar="True"
        WindowBackdropType="Mica"
        WindowCornerPreference="Round">

    <Grid>
        <Grid.Resources>
            <Style x:Key="SectionTitleStyle" TargetType="TextBlock">
                <Setter Property="FontWeight" Value="SemiBold" />
                <Setter Property="FontSize" Value="14" />
                <Setter Property="Margin" Value="0,0,0,10" />
            </Style>

            <Style x:Key="CommonComboBoxStyle" TargetType="ComboBox">
                <Setter Property="Height" Value="32" />
                <Setter Property="VerticalContentAlignment" Value="Center" />
            </Style>

            <Style x:Key="CommonButtonStyle" TargetType="Button">
                <Setter Property="Height" Value="32" />
                <Setter Property="MinWidth" Value="110" />
                <Setter Property="Padding" Value="12,0" />
            </Style>

            <Style x:Key="CommonToggleButtonStyle" TargetType="ToggleButton">
                <Setter Property="Height" Value="32" />
                <Setter Property="MinWidth" Value="130" />
                <Setter Property="Padding" Value="12,0" />
            </Style>

            <Style x:Key="SectionPanelStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="8" />
                <Setter Property="Padding" Value="12" />
                <Setter Property="Background" Value="#22000000" />
            </Style>
        </Grid.Resources>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <ui:TitleBar Grid.Row="0" Title="YModem WPF" />

        <Grid Grid.Row="1" Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="200" />
            </Grid.RowDefinitions>

            <Border Grid.Row="0" Style="{StaticResource SectionPanelStyle}" Margin="0,0,0,10">
                <StackPanel>
                    <TextBlock Text="Serial" Style="{StaticResource SectionTitleStyle}" />
                    <StackPanel Orientation="Horizontal">
                        <TextBlock VerticalAlignment="Center" Text="Port" />
                        <ComboBox x:Name="PortComboBox" Width="180" Margin="8,0,0,0" Style="{StaticResource CommonComboBoxStyle}" />
                        <Button Content="Refresh" Margin="8,0,0,0" Style="{StaticResource CommonButtonStyle}" Click="OnRefreshPortsClick" />
                        <TextBlock VerticalAlignment="Center" Margin="16,0,0,0" Text="Baud" />
                        <ComboBox x:Name="BaudRateComboBox" Width="140" Margin="8,0,0,0" IsEditable="True" Style="{StaticResource CommonComboBoxStyle}">
                            <ComboBoxItem Content="9600" />
                            <ComboBoxItem Content="19200" />
                            <ComboBoxItem Content="38400" />
                            <ComboBoxItem Content="57600" />
                            <ComboBoxItem Content="115200" />
                            <ComboBoxItem Content="230400" />
                            <ComboBoxItem Content="460800" />
                        </ComboBox>
                        <CheckBox x:Name="SendTimeoutCheckBox" Margin="16,2,0,0" IsChecked="True" Content="Send timeout" />
                        <CheckBox x:Name="ReceiveTimeoutCheckBox" Margin="12,2,0,0" IsChecked="True" Content="Receive timeout" />
                    </StackPanel>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" Margin="0,0,0,10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="16" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Style="{StaticResource SectionPanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Send" Style="{StaticResource SectionTitleStyle}" />

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,8">
                            <Button Content="Add Files" Style="{StaticResource CommonButtonStyle}" Click="OnBrowseSendFileClick" />
                            <Button Content="Clear" Margin="8,0,0,0" Style="{StaticResource CommonButtonStyle}" Click="OnClearSendFilesClick" />
                            <ToggleButton x:Name="SendActionButton" Content="Standard ToggleButton" Margin="8,0,0,0" Style="{StaticResource CommonToggleButtonStyle}" Click="OnStartSendClick" />
                        </StackPanel>

                        <ListBox x:Name="SendFilesListBox" Grid.Row="2" Margin="0,0,0,8" />

                        <ProgressBar x:Name="SendProgressBar" Grid.Row="3" Minimum="0" Maximum="100" Height="16" />
                        <TextBlock x:Name="SendStatusTextBlock" Grid.Row="4" Margin="0,6,0,6" Text="Send status: idle" />

                        <UniformGrid Grid.Row="5" Columns="2">
                            <TextBlock x:Name="SendBytesTextBlock" Text="Send bytes: 0/0" />
                            <TextBlock x:Name="SendPacketsTextBlock" Text="Send packets: 0/0" />
                        </UniformGrid>
                    </Grid>
                </Border>

                <Border Grid.Column="2" Style="{StaticResource SectionPanelStyle}">
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="Receive" Style="{StaticResource SectionTitleStyle}" />

                        <Grid Grid.Row="1" Margin="0,0,0,8">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <TextBox x:Name="SaveFolderTextBox" Grid.Column="0" Margin="0,0,8,0" Height="32" VerticalContentAlignment="Center" />
                            <Button Grid.Column="1" Content="Browse" Style="{StaticResource CommonButtonStyle}" Click="OnBrowseSaveFolderClick" />
                            <ToggleButton x:Name="ReceiveActionButton" Grid.Column="2" Content="Standard ToggleButton" Margin="8,0,0,0" Style="{StaticResource CommonToggleButtonStyle}" Click="OnStartReceiveClick" />
                        </Grid>

                        <ProgressBar x:Name="ReceiveProgressBar" Grid.Row="2" Minimum="0" Maximum="100" Height="16" Margin="0,0,0,0" />
                        <TextBlock x:Name="ReceiveStatusTextBlock" Grid.Row="3" Margin="0,6,0,6" Text="Receive status: idle" />

                        <UniformGrid Grid.Row="4" Columns="2" Margin="0,8,0,0">
                            <TextBlock x:Name="ReceiveBytesTextBlock" Text="Receive bytes: 0/0" />
                            <TextBlock x:Name="ReceivePacketsTextBlock" Text="Receive packets: 0/0" />
                        </UniformGrid>

                        <UniformGrid Grid.Row="5" Columns="2" Margin="0,8,0,0">
                            <TextBlock x:Name="ReceiveFileNameTextBlock" Text="File: -" />
                            <TextBlock x:Name="ReceiveFileDateTextBlock" Text="Date: -" />
                        </UniformGrid>
                    </Grid>
                </Border>
            </Grid>

            <Border Grid.Row="2" Style="{StaticResource SectionPanelStyle}">
                <DockPanel>
                    <TextBlock DockPanel.Dock="Top" Text="Runtime Log" Style="{StaticResource SectionTitleStyle}" Margin="0,0,0,8" />
                    <Button DockPanel.Dock="Top"
                            HorizontalAlignment="Right"
                            Content="Clear Log"
                            Margin="0,0,0,8"
                            Style="{StaticResource CommonButtonStyle}"
                            Click="OnClearLogClick" />
                    <TextBox x:Name="RuntimeLogTextBox"
                             IsReadOnly="True"
                             TextWrapping="Wrap"
                             VerticalScrollBarVisibility="Auto"
                             HorizontalScrollBarVisibility="Disabled"
                             AcceptsReturn="True" />
                </DockPanel>
            </Border>
        </Grid>
    </Grid>
</ui:FluentWindow>
